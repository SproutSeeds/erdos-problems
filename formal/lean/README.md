# Lean

Put repo-local Lean and related formal artifacts here as they migrate into `SproutSeeds/erdos-problems`.

Pack packets should eventually point at paths in this repo instead of `sunflower-coda`.
